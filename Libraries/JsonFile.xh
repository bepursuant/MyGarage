#ifndef JSONFILE_H_
#define JSONFILE_H_

#include <ArduinoJson.h>
#include "FS.h"


class JsonFile{
	public:
		String filename;

		DynamicJsonBuffer jsonBuf;
		JsonObject& json() { return jsonVar; }
		JsonObject& defaultConfig;

		bool init(String, JsonObject&);
		bool load();
		bool save();
		bool erase();

		JsonObject& get(String);

	private:
		JsonVariant jsonVar;
};

#endif